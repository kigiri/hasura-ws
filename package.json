{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test/index.js",
    "test-inspect": "node --inspect test/index.js",
    "publish": "git stash && lerna run build && git commit -am \"add build files\" && lerna publish && git stash pop"
  },
  "devDependencies": {
    "ws": "^7.2.1"
  }
}
